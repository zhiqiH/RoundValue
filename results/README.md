# results/

`step3_analyze` 与 `step4_visualize` 的离线产物：评分、标签、策略、analysis.json、CSV/HTML/SVG 图表与简短结论。

- 目录名：`YYYYMMDDHHMM_<数据集>_<拓扑>_<模型>_<hex>`（与同名 trajectories 目录精确配对）
- 完全由 `trajectories/` 离线重建，不包含密钥
- 默认纳入 Git，提交前确认无敏感内容
